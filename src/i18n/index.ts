import { de } from './de'

/**
 * Active dictionary. Spielgarten currently ships German only; swapping in
 * additional languages later means adding files alongside `de.ts`.
 */
export const t = de

export type { Dict } from './de'
